import { Component } from '@angular/core';

@Component({
    selector: 'app-icon-rotate-example',
    templateUrl: './rotate.component.html'
})
export class ThyIconRotateExampleComponent {
    rotate = 0;
}
